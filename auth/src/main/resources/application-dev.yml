spring:
  docker:
    compose:
      enabled: true
      file: auth/compose.yml
      lifecycle-management: start-only
  application:
    name: "auth"
  datasource:
    url: jdbc:postgresql://localhost:9912/auth
    username: "postgres"
    password: "postgres"
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 9922
      password: RDSpassword
server:
  port: 8091
iwag:
  services:
    wishlist:
      kafka-topics:
        - deletedUser
jwt:
  secret: "change-this-to-a-long-random-secret-key-with-at-least-32-bytes"
  expiration: 86400000 # 24h in ms